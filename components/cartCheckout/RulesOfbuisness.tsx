"use client";

import Swal from "sweetalert2";
import { AiFillAlert } from "react-icons/ai";
export default function SiteRules() {
  const showRules = () => {
    Swal.fire({
      title: "قوانین سایت",
      html: `
        <div style="text-align: right; direction: rtl; line-height: 2; font-size: 14px;">
          <h4>۱. قوانین استفاده از سایت</h4>
          <p>
            استفاده از سایت به معنی پذیرش قوانین و مقررات آن است.
            کاربران موظف هستند هنگام استفاده از خدمات، قوانین سایت را رعایت کنند.
          </p>

          <h4>۲. حساب کاربری</h4>
          <p>
            مسئولیت حفظ اطلاعات حساب کاربری و رمز عبور بر عهده کاربر است.
            اطلاعات واردشده توسط کاربر باید صحیح و معتبر باشد.
          </p>

          <h4>۳. ثبت سفارش</h4>
          <p>
            کاربر باید پیش از ثبت سفارش، مشخصات محصول، تعداد، قیمت و
            اطلاعات ارسال را بررسی کند.
          </p>

          <h4>۴. قیمت و پرداخت</h4>
          <p>
            قیمت محصولات در زمان ثبت سفارش معتبر است و پرداخت باید از
            روش‌های پرداخت موجود در سایت انجام شود.
          </p>

          <h4>۵. لغو سفارش</h4>
          <p>
            در صورت وجود مشکل در موجودی یا اطلاعات سفارش، امکان لغو سفارش
            وجود خواهد داشت.
          </p>

          <h4>۶. ارسال و تحویل</h4>
          <p>
            زمان تحویل سفارش با توجه به مقصد و روش ارسال متفاوت است.
          </p>

          <h4>۷. حریم خصوصی</h4>
          <p>
            اطلاعات کاربران برای ایجاد حساب، پردازش سفارش‌ها و ارائه خدمات
            استفاده می‌شود و جز در موارد قانونی در اختیار اشخاص ثالث قرار نمی‌گیرد.
          </p>

          <h4>۸. تغییر قوانین</h4>
          <p>
            قوانین سایت ممکن است در آینده تغییر کند و نسخه جدید پس از انتشار
            در سایت معتبر خواهد بود.
          </p>
        </div>
      `,

      showConfirmButton: false,
      showCloseButton: true,

      // با کلیک بیرون بسته می‌شود
      allowOutsideClick: true,

      width: "600px",
      customClass: {
        popup: "site-rules-popup",
      },
    });
  };

  return (
    <button onClick={showRules} className="flex flex-row justify-center items-center gap-1">
      قوانین سایت <AiFillAlert />
    </button>
  );
}